steps:
  - label: ":linux: :ruby:"
    commands:
      - bundle install
      - bundle exec rake
    expeditor:
      executor:
        docker:

  - label: ":windows: :ruby:"
    command:
      - /workdir/.expeditor/buildkite/verify.ps1
    expeditor:
      executor:
        docker:
          host_os: windows
          shell: ["powershell", "-Command"]